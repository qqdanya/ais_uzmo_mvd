# PROJECT HANDOFF: АИС УЗМО

Этот документ нужен, чтобы продолжить разработку проекта в новой итерации разработки без потери контекста. Проект уже прошел большую итерацию UX/архитектуры, поэтому главная задача нового разработчика: **сначала понять текущие решения, затем аккуратно развивать**, а не переписывать систему по догадкам.

## 1. Что это за проект

Веб-приложение на Django для учета заявок, сведений, фотографий и действий пользователей в рамках системы:

**АИС УЗМО** - автоматизированная информационная система учета заявок материального обеспечения.

Организация в интерфейсе:

**ФКУ ЦХиСО ГУ МВД России по Красноярскому краю**

Приложение предназначено для сотрудников и руководства. Интерфейс должен выглядеть серьезно, ведомственно, аккуратно и современно, без маркетинговой стилистики, без лишней декоративности и без "игрушечного" UX.

## 2. Репозиторий и текущее состояние

Текущая ветка: `main`

Последний важный коммит:

```text
b9c3238 Build custom admin panel and presence tracking
```

Перед этим были важные коммиты:

```text
0c4999b Preserve selected organ after filter reset
cf51bf3 Refine audit log presentation
8afbe7d Refine state snapshot tables
9435312 Add fire extinguisher expiry controls
a602e2b Add request table grouping modes
ee4cca3 Scope audit log by accessible departments
1fb9e92 Adjust table height behavior
```

Перед началом работы новому разработчику нужно выполнить:

```powershell
git status --short
python manage.py check
```

Если есть чужие незакоммиченные изменения, их нельзя откатывать без явной просьбы пользователя.

## 3. Как запускать

Обычный локальный запуск:

```powershell
python manage.py runserver 127.0.0.1:8000
```

Основные URL:

```text
/                         главная страница
/accounts/login/          вход
/accounts/activate/       активация учетной записи
/accounts/password-change/ смена пароля
/audit/                   журнал действий администратора
/audit/my/                журнал действий доступных пользователю отделов
/control/                 кастомная панель администратора
/admin/                   стандартная Django admin, технический резерв
```

Проверки:

```powershell
python manage.py check
python manage.py test apps.accounts apps.audit apps.requests_app
python scripts/refactor_static_check.py

Для проверки стабилизационного stage 14 отдельно:

```powershell
python manage.py test apps.accounts
```
```

`refactor_static_check.py` не заменяет Django-тесты, но быстро ловит два типовых риска после переноса кода: потерянные импорты между локальными модулями и незакрытые базовые блоки Django-шаблонов. Для точечных изменений можно запускать отдельные приложения, но перед большим коммитом лучше прогонять расширенный набор.

## 4. Структура приложений

### `apps.directory`

Справочники и фотографии:

- `TerritorialOrgan` - территориальные органы и подчиненные отделы через `parent`.
- `Department` - направления/отделы ЦХиСО.
- `TerritorialOrganPhoto` - фотографии территориального органа.
- `TerritorialOrganPhotoFolder` - папки фотографий, включая вложенные папки.

Важные решения:

- Фотографии и папки имеют мягкое удаление через `is_deleted`.
- Фотографии и папки хранят автора: `created_by`, `updated_by`.
- Есть `created_department`, чтобы ограничивать изменение/удаление фото и папок отделом автора.
- Дата фотографии - это `created_at`, пользователь ее не выбирает.
- Поиск фотографий должен искать только по имени файла и описанию, а не по полному пути.
- Папки могут быть вложенными.
- В корне раздела "Фотографии" должны показываться только элементы текущей папки, а не все фото из вложенных папок.

### `apps.requests_app`

Основные таблицы, заявки, группировки, экспорт, фотографии заявок.

Ключевые файлы:

- `models.py` - модели заявок и состояний.
- `registry.py` - конфигурация таблиц, вкладок, отделов.
- `views.py` - тонкий маршрутизирующий слой для URL. Основная логика вынесена в `services/`.
- `services/table_context.py`, `services/table_exports.py`, `services/table_filters.py` - таблицы, фильтры, группировки и экспорт.
- `services/record_forms.py`, `services/record_actions.py` - формы, сохранение и удаление заявок.
- `services/photo_responses.py`, `services/photo_assets.py`, `services/photo_asset_actions.py` - раздел фотографий.
- `services/request_photo_panels.py`, `services/request_photos.py` - фотографии, прикрепленные к заявкам.
- `forms.py` - формы таблиц.
- `permissions.py` - права пользователя по отделам и органам.
- `templates/partials/table_data.html` - отрисовка таблиц.
- `templates/partials/tables_panel.html` - панель вкладок отдела.
- `templates/partials/photos.html` - раздел фотографий.

Важные решения:

- Статуса `Новая` больше нет. Новая заявка сразу создается как `В работе`.
- `NeedStatus` содержит только:
  - `in_work` - В работе
  - `done` - Исполнена
  - `rejected` - Отклонена
- История статусов хранится через универсальную `RequestStatusHistory`.
- В таблицах кнопка истории должна отображаться только когда история действительно есть.
- Последний столбец таблиц - действия. В шапке используется иконка `bi-three-dots-vertical`, а не текст.
- В последнем столбце есть действия: редактирование, удаление, история статуса, прикрепленные фотографии.
- Массовое удаление заявок пока не делалось и не должно появляться случайно.

### `apps.audit`

Журнал действий:

- `AuditLog` хранит действие, пользователя, объект, old/new values, территориальный орган, IP, user-agent.
- `views.py` готовит человекочитаемые события через `prepare_log`.
- `audit_log` - полный журнал для администратора.
- `my_audit_log` - журнал действий в пределах доступных пользователю отделов/органов.
- `middleware.py` хранит current request и обновляет `last_seen_at` при обычных запросах.

Важные решения:

- Журнал должен быть понятен начальнику, не программисту.
- Не показывать технические ID, технические имена моделей и лишние поля, если они не нужны пользователю.
- Событие должно быть главным смысловым текстом.
- Столбец "Действие" был убран, потому что событие само объясняет действие.
- В подробностях не нужно показывать лишние блоки вроде "Дополнительные параметры отсутствуют".
- Скачивания фотографий/архивов не логируются, чтобы не засорять журнал.
- Прикрепление/открепление фотографий, изменение статуса, изменения ТМЦ должны логироваться как понятные события.

### `apps.accounts`

Пользователи, профили, активация, админ-панель.

Ключевые файлы:

- `models.py` - `UserProfile`.
- `admin.py` - кастомизация стандартной Django admin для пользователей.
- `views.py` - активация, кастомная админ-панель, presence heartbeat.
- `tests_admin_panel.py` - regression-тесты кастомной административной панели: доступы, сотрудники, presence, материальная база, настройки порогов.
- `forms.py` - активация учетной записи.
- `templatetags/account_tags.py` - отображение ФИО.

Важные решения:

- Пользователь имеет стандартную Django `User` + `UserProfile`.
- В `User` хранятся:
  - `last_name`
  - `first_name`
  - `username`
  - пароль
- В `UserProfile` хранятся:
  - `middle_name`
  - `role`
  - `allowed_organs`
  - `allowed_departments`
  - `activation_code`
  - `last_seen_at`
- В интерфейсе где возможно показывать не логин, а ФИО/инициалы.
- В бургер-меню можно показывать полное имя и отчество.
- Администратор создает пользователя без пароля и выдает логин + код активации.
- Пользователь на странице входа переходит в "Активация учетной записи", вводит логин и придумывает пароль.

Роли:

- `admin` - администратор.
- `operator` - оператор.
- `observer` - наблюдатель.

## 5. Главная страница

Главная страница состоит из трех рабочих блоков:

1. Территориальные органы.
2. Отделы.
3. Рабочая область с таблицами/фотографиями.

Ключевые UX-решения:

- На широком экране блоки располагаются горизонтально.
- На среднем масштабе/ширине возможен вариант: два блока сверху, третий ниже.
- На узкой ширине блоки складываются вертикально.
- На больших экранах блоки стараются уложиться в видимую высоту.
- При увеличении масштаба внутренние скроллы должны исчезать, чтобы не было двойных скроллов.
- Подвал должен идти после контента, не красть место у рабочих блоков.
- Первый блок может работать в режиме:
  - один территориальный орган;
  - выборочно несколько территориальных органов.
- При переключении из "Выборочно" обратно в одиночный режим нужно восстанавливать орган, выбранный до мультивыбора.
- При сбросе фильтров в таблице нельзя случайно сбрасывать выбранный территориальный орган на первый.
- Подсветка выбранного территориального органа и отдела должна быть понятной, без галочки справа.
- Подчиненные отделы в поиске органов раньше участвовали, но подсказка теперь "Поиск по территориальным органам".

Сворачивание блоков:

- Блок "Отделы" можно свернуть в блок "Территориальные органы".
- Блок "Территориальные органы" можно свернуть отдельно.
- Если оба свернуты, остается одна плавающая полупрозрачная кнопка.
- Если свернут только блок "Территориальные органы", кнопку разворота лучше показывать в блоке "Отделы", а не плавающей кнопкой.

Не ломать эту логику: она долго выверялась под разные масштабы браузера.

## 6. Таблицы и отделы

### Обеспечение товарно-материальными ценностями

Отдел: `tmc`

Вкладка: `Заявка`

Особенности:

- Одна заявка может содержать много позиций ТМЦ.
- Структура таблицы должна соответствовать ТЗ:
  - объединенный заголовок "Сведения о потребности ТМЦ";
  - под ним "Наименование", "Количество";
  - объединенный заголовок "Заявка";
  - под ним "Номер", "Дата", "Исполнение заявки";
  - "Описание";
  - действия.
- Столбцы блока "Заявка" центрируются.
- Заявки визуально разделяются жирными линиями.
- При наведении на любую позицию ТМЦ подсвечивается вся заявка.
- Есть группировки:
  - по заявкам;
  - по ТМЦ;
  - по дате;
  - по территориальному органу, только если выбрано несколько органов.
- В режиме "По ТМЦ" статусные плашки заявок не нужны.
- В режиме "По ТМЦ" экспорт должен экспортировать именно группировку ТМЦ, а не обычные заявки.
- Есть справочник ТМЦ (`TmcProduct`).
- При вводе позиции заявки работает умный поиск товаров по токенам и похожести.
- Если пользователь не выбрал подсказку, но ввел ровно существующее название, система должна связать с существующим товаром.
- Если товар не найден, он автоматически создается в справочнике.
- Если позиция не добавлена, форма должна показать ошибку, но не ломать возможность добавить позицию после ошибки.

### Автотранспортное хозяйство

Отдел: `transport`

Активные вкладки:

- `Заявка на ремонт`
- `Заявка на ГСМ`

Скрытая вкладка:

- `Автотранспорт`

Важно:

- Вкладка "Автотранспорт" скрыта в `registry.py`, но конфигурация оставлена в `HIDDEN_TABLES`, чтобы быстро вернуть при необходимости.
- В заявках есть группировки:
  - по заявкам;
  - по дате;
  - по территориальному органу.

### Отдел пожарной безопасности

Отдел: `fire`

Вкладки:

- `Огнетушители`
- `Пожарная сигнализация`
- `Охранная сигнализация`
- `Заявка`

Особенности:

- `Огнетушители`, `Пожарная сигнализация`, `Охранная сигнализация` - это не заявки, а состояние с историей изменений.
- Для таких вкладок важен режим `Последняя запись`, чтобы при выборе нескольких территориальных органов видеть актуальное состояние по каждому органу.
- Не нужна группировка по огнетушителям как по заявкам.
- В огнетушителях есть сортировка/фильтрация по сроку годности.
- Сроки годности не должны красить всю строку грубо. Используются аккуратные предупреждающие плашки/маркеры в стиле статусов.
- Заявка пожарной безопасности работает по аналогии с другими заявочными таблицами.

### Антитеррористическая укрепленность

Отдел: `antiterror`

Вкладка:

- `Заявка (акт обследования)`

Столбцы:

- Номер
- Дата
- Исполнение
- Описание
- действия

Есть группировки по заявкам, дате и территориальному органу.

### По линии ЦИТСиЗИ

Отдел: `citsizi`

Вкладка:

- `Заявка`

Столбцы:

- Номер
- Дата
- Количество
- Исполнение
- Тип техники
- Описание
- действия

Типы техники:

- Средства связи
- Организационная техника
- Вычислительная техника
- Специальная техника
- Видеонаблюдение
- Система звукового оповещения

Важно:

- `Тип техники` обязателен.
- Не должно быть варианта `--------` как нормального выбора.
- В модальных формах не должно быть двух select одновременно: кастомный и нативный. Нативный select скрывается/оборачивается кастомным контролом.

### По линии УОТО

Отдел: `uoto`

Вкладки:

- `Служебное жилье`
- `Текущий ремонт зданий, помещений, сооружений`

Особенности:

- `Служебное жилье` - состояние с историей, без статуса.
- Для служебного жилья нужен режим `Последняя запись`.
- У текущего ремонта есть подвкладка/вкладка `Заявка`.
- В заявке текущего ремонта есть столбец `Описание` после статуса.

## 7. Фотографии

Раздел фотографий открывается из блока выбранного территориального органа.

Ключевые возможности:

- загрузка одной или множества фотографий;
- drag and drop по всей области блока;
- при перетаскивании на папку загрузка идет в эту папку;
- лимит массовой загрузки: до 300 фото за один раз;
- предпросмотр фотографий в окне массовой загрузки;
- папки в виде карточек, проводниковая логика;
- вложенные папки;
- переименование папок;
- перемещение папок;
- удаление папок мягкое;
- скачивание отдельной фотографии;
- скачивание всех фотографий;
- скачивание папки архивом;
- подготовка архива не должна ломать переходы между разделами;
- есть lightbox с zoom/pan/стрелками;
- фотография в lightbox не должна обрезаться по краям;
- поиск и сортировка работают на сервере;
- сброс фильтров фотографий сбрасывает поиск/сортировку, но не должен выкидывать из текущей папки;
- путь папок кликабельный, с иконками количества фото/папок;
- в карточке папки количество фотографий и папок отображается иконками, не простым текстом;
- описание фотографии ограничено 3 строками с корректным ellipsis.

Важные подводные камни:

- Не показывать в корне фотографии из вложенных папок.
- При перемещении фотографии/папки показывать полный путь папки, иначе одинаковые названия папок запутают пользователя.
- Кнопки на карточках фото прижаты вниз.
- Кнопки на карточке папки не должны сжимать название папки.
- Нажатие на кнопки папки не должно проваливать пользователя внутрь папки.
- Нельзя физически удалять файлы с сервера при обычном удалении - используется мягкое удаление.
- Скачивание больших папок может долго готовить архив. Для архива используется потоковый/дисковый подход, чтобы не держать все в памяти.

## 8. Прикрепленные фотографии к заявкам

Есть модель `RequestPhotoLink`.

Логика:

- Фото прикрепляются к заявке при создании или редактировании через модальное окно выбора фото.
- В последнем столбце заявки есть кнопка прикрепленных фотографий, но если фото нет, кнопку можно не показывать.
- Количество прикрепленных фото показывается в tooltip, например `Прикрепленные фотографии (N шт.)`.
- В окне прикрепленных фото можно:
  - посмотреть прикрепленные фото;
  - скачать все;
  - прикрепить еще;
  - открепить фото.
- Прикрепление должно поддерживать поиск, фильтрацию и пагинацию.

## 9. Журнал действий

URL:

```text
/audit/     полный журнал администратора
/audit/my/  журнал доступных пользователю отделов
```

Дизайн:

- Таблица в стиле приложения.
- Пагинация такая же по логике, как в таблицах.
- Шапка таблицы закреплена.
- Событие отображается как основной смысловой текст.
- Действие отображается как аккуратная цветная плашка в стиле основных статусов.
- Не использовать чрезмерный `font-weight: 800`; лучше 500-600.
- Кнопка подробностей называется `Открыть`.

Фильтры:

- пользователь;
- действие;
- объект;
- отдел;
- территориальный орган;
- даты.

Ранее поиск по журналу был признан почти бесполезным и был убран.

Важные UX-решения:

- Обычный пользователь видит журнал действий людей из доступных ему отделов/органов, а не только свои действия.
- Администратор видит полный журнал.
- Для пользователя фильтры должны быть проще, но сейчас фильтр по людям и отделам допустим, потому что он может смотреть доступные отделы.
- В подробностях не надо показывать "Отдел автора" для фотографий/папок, если это выглядит лишним.
- Для событий с папкой писать `Папка фотографий`, а не `Папка с фотографиями`.
- Для удаления писать просто `Запись удалена`, без фраз вроде "скрыта из рабочих разделов".

## 10. Пользователи, доступы и безопасность

Пользователь может иметь доступ к нескольким отделам и нескольким территориальным органам.

Права:

- Администратор имеет полный доступ.
- Оператор может создавать/редактировать/удалять только в закрепленных отделах и органах.
- Наблюдатель смотрит, но не меняет.

Фотографии:

- Пользователь может изменять/удалять фото и папки только если они созданы человеком из его отдела.
- Не нужно делать сложную проверку "папка содержит чужое" при удалении, если загрузка в чужие папки запрещена и права корректно ограничены по `created_department`.

Первый вход:

- Администратор создает учетную запись без пароля.
- Система генерирует 6-значный цифровой код активации.
- Пользователь вводит логин и код, затем придумывает пароль.
- На странице входа ссылка называется `Активация учетной записи`.
- На странице активации заголовок `Регистрация`.
- Поле пароля называется `Придумайте пароль`.

Онлайн-статус:

- Браузер авторизованного пользователя отправляет heartbeat на `/accounts/presence/`.
- Интервал heartbeat: 30 секунд.
- Онлайн-окно: 1 минута.
- Админ-панель обновляется раз в 30 секунд.
- Закрытие вкладки/браузера не считается выходом из системы.
- Событие `Выход` логируется только при нажатии `Выйти из системы`.
- Если вкладка закрыта, heartbeat прекращается, и примерно через минуту пользователь станет офлайн.

## 11. Кастомная админ-панель

URL:

```text
/control/
```

Файлы:

```text
apps/accounts/views.py
templates/admin_panel/index.html
templates/admin_panel/_panel.html
static/css/app.css
static/js/app.js
```

Решение:

- Стандартная Django admin на `/admin/` остается как технический резерв.
- Пункт `Административная панель` в бургер-меню ведет на `/control/`, а не на `/admin/`.
- Внутри кастомной панели есть кнопка `Таблицы БД`.
- Панель обновляет внутренний фрагмент через HTMX polling раз в 30 секунд.
- При HTMX-запросе сервер возвращает только `admin_panel/_panel.html`, не всю страницу.
- Это важно: иначе можно случайно отрисовать футер/шапку внутри панели.

Сейчас панель показывает:

- сотрудников;
- онлайн сейчас;
- ожидающих активации;
- территориальные органы;
- заявки в работе;
- события сегодня;
- сотрудников и доступы;
- активацию;
- кто сейчас в системе;
- доступ по отделам;
- контур данных;
- последние события.

Что планировалось дальше:

- сделать полноценный современный экран управления сотрудниками;
- сделать экран управления доступами;
- добавить аналитические блоки для руководства:
  - органы с наибольшим количеством заявок;
  - заявки в работе по отделам;
  - просроченные/проблемные заявки;
  - динамика по датам;
  - частые позиции ТМЦ;
  - активность пользователей;
  - состояние заполненности данных по органам;
- не использовать слово `Топ`; лучше `Наибольшая активность`, `Сводка`, `Рейтинг по количеству заявок`, `Наибольшее количество заявок`, если нужно официальнее.

Важно:

- Не превращать админ-панель в копию стандартной CRUD admin.
- Руководству нужен обзор и контроль, а технические формы - вторым уровнем.
- Все контролы должны быть в стиле проекта.
- Не добавлять стандартные браузерные select/input без стилизации.

## 12. CSS и UI-правила

Основной CSS:

```text
static/css/app.css
```

Важно:

- Там много логики адаптивности, особенно главной страницы.
- Не делать широкие стилистические рефакторы без необходимости.
- Не удалять правила `:has(...)`, если не понимаешь, зачем они нужны.
- После изменения CSS поднять query-string версию в `templates/base.html`.
- Сейчас версия CSS/JS подключается вручную через `?v=...`.

Стиль:

- светлый, голубовато-белый ведомственный стиль;
- аккуратные панели;
- небольшие радиусы, обычно до 8px;
- мягкие плашки;
- без темных dashboard-экранов;
- без маркетинговых hero-блоков;
- без декоративных градиентных сфер/орбов;
- без огромной типографики внутри рабочих панелей;
- шрифт не делать слишком крупным;
- важная цель: все должно влезать при разных масштабах браузера.

Кнопки:

- используются Bootstrap Icons.
- Кнопки действий в таблицах и фото часто иконками.
- Tooltip должен быть аккуратным и не должен создавать горизонтальный скролл.
- Раньше были проблемы с tooltip при zoom браузера, поэтому не внедрять сложные позиционируемые тултипы без проверки.

Таблицы:

- `table-wrap` и высотная логика очень чувствительны.
- Шапки таблиц закреплены.
- На некоторых масштабах внутренние скроллы должны исчезать.
- Не добавлять лишние `max-height`/`height: 100%` без проверки на ноутбуке и большом мониторе.

Модальные окна:

- При открытом модальном окне фон не должен прокручиваться колесом.
- При этом кастомный внешний скроллбар не должен исчезать/скакать.
- В модалках формы должны быть без вложенных `<form>` внутри `<form>`. Такая ошибка уже ломала сохранение.

## 13. JavaScript

Основной файл:

```text
static/js/app.js
```

Что там есть:

- хранение выбранного органа/режима/отдела в storage;
- HTMX hooks;
- кастомные select;
- модалки;
- tooltips;
- таблицы;
- фотографии;
- lightbox;
- bulk upload;
- pagination jump;
- presence heartbeat.

Важные функции/константы:

- `PRESENCE_HEARTBEAT_MS = 30000`
- `startPresenceHeartbeat()`
- `sendPresencePing()`
- `BULK_PHOTO_BATCH_SIZE = 25`
- `ORGAN_MODE_KEY`
- `MULTI_ORGANS_KEY`
- `TABLE_STATE_PREFIX`

Подводные камни:

- HTMX-запросы включают верхнюю полосу загрузки. Presence heartbeat специально сделан через `fetch`, чтобы не дергать эту полосу.
- Не все запросы должны сохранять состояние таблиц.
- Сброс фильтров должен аккуратно чистить только состояние текущей таблицы, а не ломать выбранный орган.
- Много логики завязано на `data-*` атрибуты в шаблонах.

## 14. Экспорт

Экспорт таблиц должен учитывать текущий режим:

- обычные заявки;
- группировка по ТМЦ;
- группировка по дате;
- группировка по территориальному органу;
- множественный выбор территориальных органов.

Подводный камень:

Раньше была ошибка: в режиме выбора нескольких органов таблица показывала найденные записи со столбцом территориального органа, а экспорт выгружал больше записей и без нужного столбца. Экспорт должен соответствовать тому, что видит пользователь.

Для больших файлов:

- архивы фотографий и Excel-экспорт лучше готовить через временный файл на диске, а не держать все в RAM.

## 15. Производительность

Сейчас проект рассчитан на ведомственное локальное/корпоративное использование, но уже есть защита от очевидных проблем:

- фотографии пагинируются;
- массовая загрузка ограничена 300 фото за раз;
- bulk upload отправляет файлы пакетами;
- поиск фотографий идет на сервере;
- поиск таблиц должен срабатывать без потери фокуса;
- админ-панель обновляется фрагментом, а не всей страницей.

Если добавлять аналитику:

- не считать тяжелые агрегаты в циклах по каждой строке;
- использовать ORM aggregation;
- не делать графики/метрики, которые на каждом polling будут выполнять десятки тяжелых запросов;
- при необходимости кэшировать или обновлять только отдельные блоки.

## 16. Кодовая чистота и будущий рефакторинг

Зона технического долга уже частично разобрана:

- `apps/requests_app/views.py` сокращен до тонкого контроллера; не возвращать туда бизнес-логику без необходимости.
- `static/js/app.js` частично разнесен на отдельные JS-файлы.
- `static/css/app.css` и `static/css/admin.css` разнесены на CSS-модули через entry-файлы.

Дальше лучше:

1. Добавлять новые функции аккуратно.
2. Выносить только явно самостоятельные куски.
3. После каждого шага прогонять тесты.
4. Не менять UX одновременно с рефакторингом.

Актуальный подход к дальнейшему рефакторингу:

- не дробить `partials/table_data.html` без проверки через Django template engine; однажды разбиение этого файла сломало `{% if %}`;
- расширять существующие сервисы, а не создавать дублирующие helpers;
- добавлять тесты на таблицы, экспорт, формы заявок и фотографии;
- JS-блоки можно дальше выносить только с сохранением порядка подключения.

## 17. Тесты

Тесты есть в:

```text
apps/accounts/tests.py
apps/audit/tests.py
apps/requests_app/tests.py
```

Нужно добавлять тесты на:

- доступы;
- права администратора/оператора;
- фильтры;
- экспорт;
- группировки;
- presence heartbeat;
- HTMX-фрагменты;
- формы с кастомными select;
- корректные defaults дат.

Перед коммитом желательно:

```powershell
python manage.py test apps.accounts apps.audit apps.requests_app
python manage.py check
```

## 18. Важные недавние изменения

Последний коммит `b9c3238` добавил:

- кастомную админ-панель `/control/`;
- ссылку "Административная панель" на `/control/`;
- техническую Django admin как резерв;
- автообновление панели раз в 30 секунд;
- presence heartbeat `/accounts/presence/`;
- heartbeat клиента раз в 30 секунд;
- онлайн-окно 1 минута;
- тесты на админ-панель и presence;
- дефолт дат в таблицах: если записей нет, дата с/по по умолчанию сегодня.

## 19. Как объяснять новому разработчику следующую задачу

Хороший вводный промпт:

```text
Ты продолжаешь Django-проект АИС УЗМО.
Перед началом прочитай PROJECT_HANDOFF.md.
Не переписывай архитектуру и не меняй UX без необходимости.
Сохраняй текущий стиль приложения.
Стандартная Django admin остается техническим резервом.
Кастомная админ-панель находится на /control/.
После изменений запускай python manage.py check и релевантные тесты.
Если видишь странную реализацию, сначала проверь handoff и существующие тесты: возможно, это осознанное UX-решение.
```

Плохой промпт:

```text
Сделай полный рефакторинг и улучши все.
```

Такой промпт опасен: новый разработчик может удалить важную адаптивную логику, упростить права, сломать таблицы, заменить кастомные контролы стандартными или переписать существующий UX.


## Исправление после stage 13

После прогона `python manage.py test apps.requests_app` исправлены два регресса стабилизационного этапа:

- оператор без явно заданных отделов снова считается не ограниченным по отделам; если отделы назначены явно, доступ ограничивается только ими;
- в picker прикрепления фотографий уже выбранные фотографии остаются видимыми даже при поиске/фильтре по папке.

## 20. Главные "не ломать"

1. Не ломать трехколоночную главную страницу и ее адаптивность.
2. Не ломать режим "Один/Выборочно" для территориальных органов.
3. Не возвращать статус заявки `Новая`.
4. Не показывать скрытую вкладку `Автотранспорт`, если пользователь не попросил.
5. Не выводить фотографии из вложенных папок в корне.
6. Не удалять физически фотографии при обычном удалении.
7. Не делать стандартные некрасивые select/input там, где уже есть кастомный стиль.
8. Не показывать технические поля в журнале действий для руководства.
9. Не превращать `/control/` в копию `/admin/`.
10. Не менять интервал heartbeat и онлайн-окно без явного решения.
11. Не делать массовые удаления без отдельного обсуждения.
12. Не использовать слово `Топ` в интерфейсе руководства без причины; стиль должен быть официальнее.
13. Не делать большие CSS/JS refactor вместе с функциональными изменениями.
14. Не откатывать чужие незакоммиченные изменения.

## 21. Рекомендуемый порядок развития админ-панели

1. Экран/блок "Сотрудники":
   - список сотрудников;
   - статус онлайн;
   - роль;
   - отделы;
   - территориальные органы;
   - состояние активации;
   - быстрый переход к редактированию.

2. Экран/блок "Доступы":
   - сотрудники по отделам;
   - сотрудники по территориальным органам;
   - предупреждения о пользователях без отдела/органа;
   - массовое назначение доступа, но осторожно.

3. Аналитика руководителя:
   - количество заявок в работе по отделам;
   - количество исполненных/отклоненных;
   - органы с наибольшим количеством заявок;
   - динамика по датам;
   - частые позиции ТМЦ;
   - проблемные/долго не меняющиеся заявки.

4. Состояние данных:
   - где давно не обновлялись состояния;
   - где нет последних записей по состояниям;
   - где мало/нет фотографий;
   - где есть ожидающие активации сотрудники.

5. Только после этого:
   - перенос части функций из стандартной Django admin в кастомный UI.

## 22. Если что-то кажется странным

Перед изменением проверь:

- историю в `git log`;
- тесты;
- `PROJECT_HANDOFF.md`;
- существующие шаблоны;
- связанные CSS/JS data-атрибуты.

Многие "странности" появились после конкретных UX-багов:

- двойные скроллы при масштабировании;
- tooltip уезжал при zoom;
- модалка разрешала скроллить фон;
- форма ломалась из-за вложенной формы;
- кастомный select дублировался нативным;
- сброс фильтров сбивал выбранный орган;
- экспорт не совпадал с режимом таблицы;
- фото из вложенных папок показывались в корне.

Не надо "упрощать" эти места без проверки.

## 23. Stage 13 additional test-fix notes

Повторный прогон `python manage.py test apps.requests_app` показал оставшийся хвост: не создавалась `RequestStatusHistory` при смене статуса и picker фотографий не показывал найденную фотографию из папки при поиске из корня.

Причины и решения:

- `ModelForm.is_valid()` мутирует переданный `instance` до вызова `form.save(commit=False)`. Поэтому сервисы сохранения теперь получают старые значения из БД перед сохранением и используют их для status history и аудита.
- `request_photo_picker_context()` при поиске из корня теперь ищет по всей библиотеке фотографий органа, включая вложенные папки. Уже выбранные фотографии по-прежнему добавляются в выдачу поверх фильтра.

## Stage 20 refactor notes

Stage 20 продолжил внутреннюю чистку существующей кастомной админ-панели без изменения UI, маршрутов и бизнес-поведения.

Изменения:

- `apps/accounts/admin_assets.py` разложен на более мелкие helper-функции для фильтров, вкладок состояния, пагинационных hidden fields, сортировки и фильтрации строк матрицы.
- Матрица материальной базы для `/control/assets/` теперь строится один раз, после чего статусные фильтры применяются к уже подготовленным строкам. Раньше общий набор и видимый набор строк собирались раздельно, что повторяло запросы к тем же таблицам.
- Деталка категории `/control/assets/<category>/` также переиспользует уже подготовленные строки для фильтра по состоянию, не пересобирая матрицу повторно.
- Для pagination fields используется общий `build_pagination_fields()` из `admin_common.py`.
- Добавлены regression-тесты на фильтрацию материальной базы по категории, состоянию и поиску, а также на то, что фильтр состояния в деталке категории не меняет итоговую summary-сводку.

Проверено:

```text
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test apps.accounts --noinput
python manage.py test apps.audit --noinput
python manage.py test apps.requests_app.tests_refactor_safety --noinput
python scripts/refactor_static_check.py
```

Полный `python manage.py test apps.requests_app --noinput` в среде ChatGPT снова не завершился до таймаута; до таймаута тесты шли без падений.

## Stage 21 refactor notes

Stage 21 продолжил внутреннюю чистку существующего раздела сотрудников кастомной админ-панели без добавления новых маршрутов, вкладок или бизнес-функций.

Изменения:

- `apps/accounts/admin_employees.py` разложен на более мелкие helper-функции для поиска, фильтрации по правам, фильтрации по активности/активации и построения вкладок.
- Счётчики KPI и вкладок сотрудников теперь считаются одним агрегированным запросом через `Count(..., filter=...)` вместо серии отдельных `.count()`.
- `employee_presence_payload()` переиспользует один queryset и один набор агрегированных счётчиков вместо повторных вызовов `employee_queryset()` для каждой вкладки.
- Pagination hidden fields переведены на общий `build_pagination_fields()` из `admin_common.py`.
- Исправлено отображение и фильтрация полного доступа по отделам: пустой список `allowed_departments` в проекте уже означает отсутствие ограничения по отделам, поэтому UI теперь показывает «Все отделы», а фильтр по отделу включает таких сотрудников.
- Добавлены regression-тесты на фильтр сотрудников по отделу с полным доступом и на отображение полного доступа по отделам в карточке сотрудника.

Проверено:

```text
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test apps.accounts --noinput
python manage.py test apps.audit --noinput
python manage.py test apps.requests_app.tests_refactor_safety --noinput
python scripts/refactor_static_check.py
```

Полный `python manage.py test apps.requests_app --noinput` в среде ChatGPT снова не завершился до таймаута; до таймаута тесты шли без падений.

## Stage 24 refactor notes

Stage 24 закрывает пункт по унификации кастомных выпадающих списков без изменения UI, маршрутов и бизнес-поведения.

Изменения:

- Добавлен общий partial `templates/partials/admin_multiselect.html` для admin dropdown/multiselect.
- Добавлен inclusion tag `apps/accounts/templatetags/admin_select_tags.py`, который нормализует варианты checkbox/radio фильтров и рендерит общий partial.
- Ручная разметка `admin-multiselect` удалена из существующих шаблонов админ-панели: заявки, органы, отделы, материальная база, сотрудники и форма сотрудника теперь используют общий компонент.
- Отдельный `request-photo-sort-select` заменён обычным `select.form-select`, который автоматически обрабатывается общим `static/js/custom_select.js`.
- Из `custom_select.js` удалена отдельная ветка JS для `request-photo-sort-select`; сортировка фотографий теперь использует общий single-select обработчик.
- Добавлены regression-тесты, которые проверяют централизованную разметку `admin-multiselect`, отсутствие отдельного `request-photo-sort-select` и успешный рендер ключевых страниц админ-панели.

Проверено:

```text
python manage.py check
python manage.py makemigrations --check --dry-run
python scripts/refactor_static_check.py
python manage.py collectstatic --dry-run --noinput
python manage.py test apps.accounts --noinput
python manage.py test apps.audit --noinput
python manage.py test apps.requests_app.tests_refactor_safety --noinput
```

Полный `python manage.py test` и полный `python manage.py test apps.requests_app` в среде ChatGPT снова не завершились до таймаута. Чтобы проверить фактический набор, `apps.requests_app.tests` был прогнан по чанкам: все 108 тестов `apps.requests_app.tests` прошли без падений.

## Stage 27 — ограничения данных и metadata фотографий

Сделано:
- Добавлен `MinValueValidator(0)` и DB `CheckConstraint` для `TerritorialOrgan.order_number`, чтобы порядок территориального органа не мог быть отрицательным.
- В `TerritorialOrganPhoto` добавлены служебные поля `file_size` и `mime_type`.
- При сохранении фотографии автоматически обновляются `original_filename`, `file_size`, `mime_type`.
- Добавлена миграция `apps/directory/migrations/0008_territorialorganphoto_file_size_and_more.py` с заполнением metadata для уже существующих фотографий.
- В Django admin для фотографий metadata добавлены в список/фильтр/readonly-поля.
- Добавлены tests в `apps/directory/tests.py` для отрицательного `order_number` и metadata фотографий.

Проверки:
- `python manage.py check` — OK.
- `python manage.py makemigrations --check --dry-run` — OK.
- `python scripts/refactor_static_check.py` — OK.
- `python manage.py collectstatic --dry-run --noinput` — OK.
- `python manage.py test apps.accounts --noinput` — OK, 40 tests.
- `python manage.py test apps.audit --noinput` — OK, 18 tests.
- `python manage.py test apps.directory --noinput` — OK, 3 tests.
- `python manage.py test apps.requests_app.tests_refactor_safety --noinput` — OK, 7 tests.
- `apps.requests_app.tests.AppFlowTests` — OK по чанкам, 107 tests.
- `apps.requests_app.tests.SeedCommandTests` — OK, 1 test.

Примечание:
- Проверка реального MIME/содержимого загружаемого файла остаётся отдельным production-hardening этапом; в stage 27 добавлено именно хранение metadata.

## Stage 28 — strict photo upload validation

- Исправлен слишком строгий regression-тест `test_single_select_markup_is_centralized`: теперь он допускает только системные raw-select partials `templates/partials/table/_summary_actions.html` и `templates/partials/table/_toolbar.html`, если они есть в локальной ветке, и продолжает запрещать неожиданные ручные `<select>` в остальных шаблонах.
- Добавлена проверка реального содержимого загружаемых фото через Pillow: разрешены только фактические изображения JPEG, PNG и WEBP.
- MIME-тип фото теперь определяется по содержимому файла, а не только по имени/данным браузера.
- Добавлена миграция `apps/directory/migrations/0009_alter_territorialorganphoto_image.py` для фиксации нового валидатора поля `image`.
- Добавлены тесты: поддельный `.jpg` с текстовым содержимым отклоняется модельной валидацией и формой; валидное изображение с неверным browser `content_type` сохраняется с реальным MIME.

## Stage 31 — frontend module split

- Split TMC product suggestion UI helpers from `static/js/app.js` into `static/js/tmc_products.js`.
- Split download preparation indicator helpers from `static/js/app.js` into `static/js/download_preparing.js`.
- Kept `app.js` as the main entry point and event orchestration file; no UI behavior or routes changed.
- Updated `templates/base.html` script loading order so helper modules are loaded before `app.js`.
- Added a regression test to keep these helper blocks outside `app.js` and ensure script order is correct.
- Static checks completed in this environment: `python scripts/refactor_static_check.py`, `node --check static/js/app.js`, `node --check static/js/tmc_products.js`, `node --check static/js/download_preparing.js`, `python -m compileall`.
- Django checks/tests were not run in this environment because Django is not installed here (`ModuleNotFoundError: No module named 'django'`).

## Stage 34: refactor audit views

Журнал действий больше не держит всю подготовку контекста и человекочитаемых событий в `apps/audit/views.py`.

Новая структура:

- `apps/audit/views.py` — тонкие контроллеры `/audit/`, `/audit/my/`, `/audit/<pk>/`.
- `apps/audit/services/constants.py` — общие константы журнала, подписи действий, группы моделей.
- `apps/audit/services/filters.py` — права видимости журнала, фильтры, даты, pagination fields.
- `apps/audit/services/display.py` — `prepare_log()`, человекочитаемые события, изменения полей, user-agent, статусная история.
- `apps/audit/services/page_context.py` — сбор контекста и render общего шаблона журнала.

Важно: `prepare_log` по-прежнему реэкспортируется из `apps.audit.views`, потому что его используют `apps/accounts/views.py` и `apps/accounts/admin_employees.py`.

## Stage 35 — requests_app tests split

- Бывший монолитный `apps/requests_app/tests.py` (~2500 строк) заменён маленьким compatibility-файлом с описанием новой структуры.
- Общие фикстуры и helper-методы вынесены в `apps/requests_app/tests_base.py`.
- Тесты разделены по тематическим модулям:
  - `apps/requests_app/tests_core.py` — dashboard, прямые URL, базовые права доступа;
  - `apps/requests_app/tests_tmc.py` — заявки ТМЦ, подсказки товаров, группировки, фильтры и экспорты ТМЦ;
  - `apps/requests_app/tests_tables.py` — остальные таблицы заявок/учёта, статусы, экспорты, валидация;
  - `apps/requests_app/tests_photos.py` — библиотека фото, папки, bulk upload, request-photo picker/lightbox;
  - `apps/requests_app/tests_seed.py` — idempotent seed command.
- Добавлен regression-тест в `tests_refactor_safety.py`, который фиксирует, что большой `tests.py` не вернулся обратно.
- Команда для полного набора по приложению остаётся: `python manage.py test apps.requests_app`.
- Для точечных прогонов теперь можно использовать тематические модули, например `python manage.py test apps.requests_app.tests_photos`.

## Stage 36 — local runtime gitignore cleanup

- `.gitignore` now explicitly ignores dashboard threshold runtime files:
  - `dashboard_thresholds.json`
  - `dashboard_thresholds.json.tmp`
- These files are local/runtime configuration artifacts and should not be committed.
- Existing runtime exclusions remain in place: `.env`, `db.sqlite3`, `media/`, `staticfiles/`, `__pycache__/`, `*.pyc`.

## Stage 37 — table_data template split

- `templates/partials/table_data.html` is now a small dispatcher template instead of a 371-line mixed table template.
- Table UI is split into focused partials under `templates/partials/table/`:
  - `_nested_tabs.html`
  - `_toolbar.html`
  - `_active_filters.html`
  - `_summary.html`
  - `_actions.html`
  - `_pagination.html`
  - `_rows.html`
  - `_rows_tmc_product_grouped.html`
  - `_rows_organ_grouped.html`
  - `_rows_date_grouped.html`
  - `_rows_tmc_default.html`
  - `_rows_default.html`
- No URL, context key, UI behavior, or business logic was intentionally changed.
- Added regression tests to keep `table_data.html` delegated to partials and prevent the large table template from returning.
